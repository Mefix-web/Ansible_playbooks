Welcome to:
{{figlet.stdout}}
This host is managed by Ansible, last run: {{ansible_date_time.iso8601}}, distro: {{ansible_distribution}} {{ansible_distribution_version}}
IP adresses:
{% for ip in ansible_all_ipv4_addresses %}
  - {{ip}}
{%endfor%}
